pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            name = 'Fabric'
            url = 'https://maven.fabricmc.net/'
        }
        maven {
            name = 'NeoForged'
            url = 'https://maven.neoforged.net/releases'
        }
        maven {
            name = 'Sponge Snapshots'
            url = 'https://repo.spongepowered.org/repository/maven-public/'
        }
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = "${mod_name}"
// The loader modules cannot resolve their AssortedLib artifact until it is published, so
// `-PcommonOnly` builds just the common module. This exists so nobody has to edit this file to do
// that: concurrent edit-and-restore races corrupt it.
if (startParameter.projectProperties.containsKey('commonOnly')) {
    include("common")
} else {
    include("common", "fabric", "neoforge")
}
